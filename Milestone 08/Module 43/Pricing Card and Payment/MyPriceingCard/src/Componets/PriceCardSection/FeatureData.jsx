import React from "react";

const FeatureData = ({ featurevalue }) => {
  console.log(featurevalue);
  return (
    <div>
      <p> {featurevalue} </p>
    </div>
  );
};

export default FeatureData;
